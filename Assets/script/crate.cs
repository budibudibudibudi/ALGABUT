using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class crate : MonoBehaviour
{
    // Start is called before the first frame update
    void OnEnable()
    {
        this.transform.name = "Crate";
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
